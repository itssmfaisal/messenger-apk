// Root project build file.
// Do NOT apply Android application or Kotlin Android plugins here —
// they should be applied in the module-level build files (for example, `app/build.gradle.kts`).
// Add any root-level configuration (if needed) below.

// ...existing code...
